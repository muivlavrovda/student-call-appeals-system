import pytest


@pytest.fixture(autouse=True)
def _ai_disabled_by_default(settings):
    """По умолчанию ИИ в тестах выключен.

    Конфигурация подключения берётся из ``.env`` и на рабочей машине может быть
    задана (с реальным ключом). Чтобы тесты не зависели от окружения и не делали
    сетевых вызовов, по умолчанию отключаем ИИ; тесты ИИ включают его сами через
    собственную фикстуру.
    """
    settings.AI_CONFIG = None
